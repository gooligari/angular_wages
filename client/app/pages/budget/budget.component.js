import template from './budget.html';
import controller from './budget.controller';
import './budget.scss';

let budgetComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default budgetComponent;
